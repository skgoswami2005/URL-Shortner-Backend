<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <Style>
        html{
            font-family: Arial, Helvetica, sans-serif;
        }

        .container {
            display: flex;
            justify-content: center;
            flex-direction: column;
            width: 100%;
            height: 100vh;
        }

        .loader {
            border: 12px solid #f3f3f3;
            /* Light grey */
            border-top: 12px solid #0984d6;
            /* Blue */
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 2s linear infinite;
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }
    </Style>
</head>

<body>
    <div class="container">
        <div class="loader" style="margin: 10px auto;"></div>
        <p style="margin: 10px auto;"">Redirecting to...</p>
        <h2 id="url" style="margin: 10px auto; color: rgb(61, 61, 61)"><%- url %></h2>
    </div>

    <script>
        const url = document.getElementById("url").innerHTML;
        console.log(url);

        setTimeout(() => {
            window.location.href = url;
        }, 3000);

    </script>
</body>

</html>